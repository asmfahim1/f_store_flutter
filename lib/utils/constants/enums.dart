enum TextSizes { SMALL, MEDIUM, LARGE }

enum OrderStatus { PROCESSING, SHIPPED, DELIVERED }

enum PaymentMethods {
  PAYPAL,
  GOOGLEPAY,
  APPLEPAY,
  VISA,
  MASTERCARD,
  CREDITCARD,
  PAYSTACK,
  PAYTM
}
